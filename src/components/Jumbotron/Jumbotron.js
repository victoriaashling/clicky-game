import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
  <header className="jumbotron text-center">
    <h1>Clicky Game</h1>
    <h4>Each click earns a point, but if you click on the same image more than once you lose!</h4>
  </header>
);

export default Jumbotron;